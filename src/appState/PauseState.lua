Class = require'src.Class'
AppState = require'src.appState.AppState'

local PauseState = Class(AppState)



-- for drawing perhaps get the gameState draw function somehow and draw it
-- even though there will be no updateing and then draw the pause stuff over top of it

return PauseState